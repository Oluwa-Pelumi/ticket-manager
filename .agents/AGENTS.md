# Project Rules

## Commands
- Do NOT run `npm` commands (e.g. `npm install`, `npm run dev`, `npm run build`).
- Do NOT run test commands (e.g. `php artisan test`, `phpunit`, `vendor/bin/phpunit`).
- Only make code/file changes and leave commands for the user to run manually.
